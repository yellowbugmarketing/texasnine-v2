// Import the React library, which is necessary for creating React components.
import React from "react";

// Define a functional component called Loader.
const Loader = () => {
  // Return a simple JSX element that displays "Loader."
  return <div>Loader</div>;
};

// Export the Loader component as the default export for this module.
export default Loader;
